import { Injectable, Logger, OnModuleInit } from '@nestjs/common';
import { Processor, WorkerHost } from '@nestjs/bullmq';
import { Job } from 'bullmq';
import { EmailService } from './email.service';

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  } from '@nestjs/common';
import { Processor, WorkerHost }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  } from '@nestjs/bullmq';
import { Job }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  } from 'bullmq';
import { EmailService }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  } from './email.service';

@Injectable()
@Processor('email')
export class EmailProcessor extends WorkerHost implements OnModuleInit {
  private readonly logger = new Logger(EmailProcessor.name);
  
  constructor(private readonly emailService: EmailService) {
    super(); // Call the WorkerHost constructor
    this.logger.log('🔍 Email processor initializing...');
  }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * NestJS lifecycle hook called when the module is initialized
   */
  onModuleInit() {
    this.logger.log('💬 Email processor ready - NestJS configuration complete');
    this.logger.log(`💬 Redis connection: ${process.env.REDIS_HOST || 'localhost'}:${process.env.REDIS_PORT || '6379'}`);
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }:${process.env.REDIS_PORT || '6379'}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`);
    this.logger.log('🔔 NOTA: El procesador de email está correctamente configurado como worker');
    this.logger.log('📰 IMPORTANTE: Verifica que no aparezcan advertencias de "Redis minimum version"');
    this.logger.log('⏰ El worker está esperando trabajos de email...');
  }
}
  }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }
  
  // Eliminamos el método clearQueueOnStartup ya que no tenemos acceso a getQueue directamente

  // Este método es llamado automáticamente por BullMQ para procesar cada trabajo
  async process(job: Job<any>): Promise<void> {
    this.logger.log(`📧 Procesando email: ${job.name}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  } (ID: ${job.id}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  })`);
    const { email, token }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  } = job.data;
    
    this.logger.log(`📧 Datos: email=${email}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }, token=${token.substring(0, 8)}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }...`);
    this.logger.log(`📧 Frontend URL: ${process.env.FRONTEND_URL || 'http://localhost:3000'}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`);
  
    try {
      switch (job.name) {
        case 'sendPasswordResetEmail':
          this.logger.log('📧 Procesando email de reseteo de contraseña');
          await this.handlePasswordReset(email, token);
          this.logger.log('✅ Email de reseteo enviado exitosamente');
          break;
          
        case 'sendEmailVerificationEmail':
          this.logger.log('📧 Procesando email de verificación');
          await this.handleEmailVerification(email, token);
          this.logger.log('✅ Email de verificación enviado exitosamente');
          break;

        case 'sendTwoFactorCodeEmail':
          this.logger.log('📧 Procesando email con código de verificación 2FA');
          await this.handleTwoFactorCode(job.data.email, job.data.code);
          this.logger.log('✅ Email con código 2FA enviado exitosamente');
          break;

        case 'sendSecurityNotificationEmail':
          this.logger.log('📧 Procesando email de notificación de seguridad');
          await this.handleSecurityNotification(
            job.data.email, 
            job.data.subject || 'Alerta de seguridad', 
            job.data.text
          );
          this.logger.log('✅ Email de notificación de seguridad enviado exitosamente');
          break;
          
        default:
          this.logger.warn(`⚠️ Tipo de trabajo desconocido: ${job.name}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`);
          break;
      }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }
      
      return; // Trabajo completado exitosamente
      
    }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  } catch (error: any) {
      this.logger.error(`❌ Error procesando email ${job.name}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }:`, error?.message || 'error desconocido');
      throw error; // Re-lanzar para que BullMQ maneje el error
    }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }
  }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }

  private async handlePasswordReset(email: string, token: string): Promise<void> {
    this.logger.log(`📧 Procesando reseteo de contraseña para: ${email}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`);
    
    const resetLink = `${process.env.FRONTEND_URL || 'http://localhost:3000'}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }/reset-password/${token}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`;
    this.logger.log(`🔗 Link de reseteo generado: ${resetLink}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`);
    
    try {
      const htmlContent = `
        <h1>Reset your password</h1>
        <p>Click the button below to reset your password:</p>
        <a href="${resetLink}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }" style="display: inline-block; background-color: #4CAF50; color: white; padding: 10px 20px; text-decoration: none; border-radius: 5px;">Reset Password</a>
        <p>If the button doesn't work, copy and paste this link in your browser:</p>
        <p>${resetLink}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }</p>
        <p>This link will expire in 1 hour.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Reset your password',
        `Click the link to reset your password: ${resetLink}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`,
        htmlContent
      );
      
      this.logger.log(`✅ Email de reseteo enviado a ${email}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`);
    }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  } catch (error: any) {
      this.logger.error(`❌ Error al enviar email de reseteo a ${email}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }:`, error?.message || error);
      throw error;
    }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }
  }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }

  private async handleEmailVerification(email: string, token: string): Promise<void> {
    this.logger.log(`📧 Procesando verificación para: ${email}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`);
    
    const verificationLink = `${process.env.FRONTEND_URL || 'http://localhost:3000'}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }/auth/verify-email/${token}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`;
    this.logger.log(`🔗 Link de verificación generado: ${verificationLink}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`);
    
    try {
      const htmlContent = `
        <h1>Verify your email</h1>
        <p>Click the button below to verify your email:</p>
        <a href="${verificationLink}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }" style="display: inline-block; background-color: #2196F3; color: white; padding: 10px 20px; text-decoration: none; border-radius: 5px;">Verify Email</a>
        <p>If the button doesn't work, copy and paste this link in your browser:</p>
        <p>${verificationLink}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }</p>
        <p>This link will expire in 24 hours.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Verify your email',
        `Click the link to verify your email: ${verificationLink}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`,
        htmlContent
      );
      
      this.logger.log(`✅ Email de verificación enviado a ${email}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }`);
    }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  } catch (error: any) {
      this.logger.error(`❌ Error al enviar email de verificación a ${email}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }:`, error?.message || 'error desconocido');
      throw error;
    }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }
  }

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }
}

  /**
   * Process jobs from the email queue
   */
  async process(job: Job): Promise<any> {
    const { name, data } = job;
    this.logger.log(`⚙️ Processing email job: ${name}`);
    
    try {
      switch (name) {
        case 'twoFactorCode':
          await this.handleTwoFactorCode(data.email, data.code);
          break;
        case 'securityNotification':
          await this.handleSecurityNotification(data.email, data.subject, data.text);
          break;
        default:
          this.logger.warn(`🟡 Unsupported email job type: ${name}`);
          throw new Error(`Unsupported email job type: ${name}`);
      }
      
      return { success: true };
    } catch (error: any) {
      this.logger.error(`❌ Error processing email job ${name}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends two-factor authentication code email
   */
  private async handleTwoFactorCode(email: string, code: string): Promise<void> {
    this.logger.log(`📧 Enviando código 2FA a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Código de verificación</h1>
        <p>Tu código de verificación de dos factores es:</p>
        <div style="background-color: #f0f0f0; padding: 15px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;">
          ${code}
        </div>
        <p>Este código expirará en 10 minutos.</p>
        <p>Si no solicitaste este código, ignora este correo. Alguien podría estar intentando acceder a tu cuenta.</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        'Código de verificación de dos factores',
        `Tu código de verificación de dos factores es: ${code}. Este código expirará en 10 minutos.`,
        htmlContent
      );
      
      this.logger.log(`✅ Email con código 2FA enviado a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar código 2FA a ${email}:`, error?.message || error);
      throw error;
    }
  }

  /**
   * Sends security notification emails
   */
  private async handleSecurityNotification(email: string, subject: string, text: string): Promise<void> {
    this.logger.log(`📧 Enviando notificación de seguridad a: ${email}`);
    
    try {
      const htmlContent = `
        <h1>Notificación de seguridad</h1>
        <p>${text}</p>
        <p>Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.</p>
        <p>Fecha y hora: ${new Date().toLocaleString()}</p>
      `;
      
      await this.emailService.sendEmail(
        email,
        subject,
        `${text} Si no realizaste esta acción, por favor contacta inmediatamente con soporte para asegurar tu cuenta.`,
        htmlContent
      );
      
      this.logger.log(`✅ Notificación de seguridad enviada a ${email}`);
    } catch (error: any) {
      this.logger.error(`❌ Error al enviar notificación de seguridad a ${email}:`, error?.message || error);
      throw error;
    }
  }
